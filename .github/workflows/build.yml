name: Test con Docker

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Construir la imagen de Docker
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: my-java-app:latest

      - name: Ejecutar tests dentro del contenedor
        run: docker run --rm my-java-app:latest

      # Copiar reportes HTML fuera del contenedor
      - name: Copiar reportes de tests
        run: |
          id=$(docker create my-java-app:latest)
          docker cp $id:/app/build/reports ./reports
          docker rm -v $id

      # Subir reportes como artefacto descargable
      - name: Subir reportes como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: ./reports
